# 组合测试表生成器：需求规格说明书

## 1. 项目背景

软件测试常包含多个因子与多个水平。全排列会使 Case 数量快速膨胀。组合测试表生成器用于根据用户输入的因子和水平，自动生成较少但覆盖充分的测试组合表。

第一优先级是生成两两覆盖表（pairwise covering array）：任意两个因子的所有有效水平组合至少出现一次。第二优先级是：当输入满足严格正交表条件、且本地存在对应参考表时，使用严格正交表（orthogonal array）。

## 2. 项目目标

构建一个独立命令行程序。用户输入自定义因子和水平后，可得到：

1. 组合测试 Case 表；
2. 生成方法说明；
3. 两两覆盖率；
4. 严格正交表的参考来源或降级原因；
5. JSON、Markdown、CSV 格式输出。

程序必须可离线运行。两两覆盖生成不依赖网络；严格正交表使用随程序内置的参考数据。

## 3. 非目标

第一版不实现：Web 页面、数据库、AI 自动理解自然语言因子、三因子及以上覆盖、复杂业务约束求解、保证两两覆盖表绝对最少行数，或任意输入都强行生成严格正交表。

## 4. 核心概念

### 4.1 因子与水平

- 因子：需要变化的测试条件，例如“订单状态”。
- 水平：该因子的可选值，例如“已送达”“超期”。

### 4.2 两两覆盖表

任意选择两个因子时，这两个因子的任意水平组合至少出现一次。它不要求每种组合出现次数相同。

### 4.3 严格正交表

严格正交表要求任意两个因子的每种水平组合出现次数完全相同，通常写作 `OA(N, k, s, t)`：`N` 为 Case 数，`k` 为因子数，`s` 为每因子的水平数，`t` 为覆盖强度。第一版只支持 `t=2`。

严格正交表一定满足两两覆盖；两两覆盖表不一定是严格正交表。

## 5. 总体设计

```text
JSON 输入文件
    ↓
参数校验
    ↓
模式选择
├── orthogonal：仅查严格正交表
├── pairwise：直接生成两两覆盖表
└── auto：优先严格正交表，无法匹配则生成两两覆盖表
    ↓
覆盖率验证
    ↓
JSON / Markdown / CSV 输出
```

## 6. 输入格式

第一版仅接受 UTF-8 JSON 文件。

```json
{
  "mode": "auto",
  "strength": 2,
  "factors": [
    {
      "name": "用户身份",
      "levels": ["本人", "他人", "未知用户"]
    },
    {
      "name": "订单状态",
      "levels": ["已送达", "超期", "已退款", "未送达"]
    },
    {
      "name": "退款金额",
      "levels": ["全额", "部分退款", "超过余额", "非法金额"]
    }
  ]
}
```

| 字段 | 必填 | 含义 |
| --- | ---: | --- |
| `mode` | 否 | `auto`、`orthogonal`、`pairwise`；默认 `auto` |
| `strength` | 否 | 覆盖强度；第一版仅支持 `2` |
| `factors` | 是 | 因子数组，至少 2 个因子 |
| `factors[].name` | 是 | 因子名称，必须唯一 |
| `factors[].levels` | 是 | 水平数组，至少 2 个且不允许重复 |

模式说明：

| 模式 | 行为 |
| --- | --- |
| `pairwise` | 直接生成两两覆盖表 |
| `orthogonal` | 只使用严格正交表；没有匹配参考表则报错 |
| `auto` | 优先查严格正交表；无法匹配时生成两两覆盖表，并说明降级原因 |

## 7. 输出格式

### 7.1 JSON

```json
{
  "method": "pairwise_covering_array",
  "case_count": 12,
  "coverage": {
    "strength": 2,
    "required_combinations": 48,
    "covered_combinations": 48,
    "coverage_rate": 1.0
  },
  "orthogonal_array": null,
  "warnings": [
    "当前因子水平数不一致，无法使用等水平严格正交表，已生成两两覆盖表。"
  ],
  "test_cases": [
    {
      "case_id": "case_001",
      "用户身份": "本人",
      "订单状态": "已送达",
      "退款金额": "全额"
    }
  ]
}
```

若使用严格正交表，结果必须额外包含 `array_id`、行数、因子数、水平数、强度和参考数据来源。

### 7.2 表格输出

Markdown 和 CSV 均以一行一个 Case 输出，第一列为 `case_id`，其余列保持原始因子名称和水平名称。

## 8. 严格正交表参考数据

程序内置常见的二阶正交表参考数据，保存来源、参数、原始矩阵和校验结果。

### 8.1 数据来源与分工

- **Sloane Orthogonal Array Library**：V0.2 下载严格正交表原始矩阵的来源。
- **NIST ACTS / Covering Array Tables**：两两覆盖生成、覆盖率校验与后续约束组合测试的方法参考；不作为严格 OA 原始矩阵的下载来源。

下载后的每张严格 OA 都必须由程序自行验证；来源可信不代替本地校验。

| 水平数 | 因子数范围 | 参考 OA |
| ---: | --- | --- |
| 2 | 2～3 | `OA(4,3,2,2)` |
| 2 | 4～7 | `OA(8,7,2,2)` |
| 2 | 8 | `OA(12,11,2,2)` |
| 3 | 2～4 | `OA(9,4,3,2)` |
| 3 | 5～7 | `OA(18,7,3,2)` |
| 3 | 8 | `OA(27,13,3,2)` |
| 4 | 2～5 | `OA(16,5,4,2)` |
| 4 | 6～8 | `OA(32,9,4,2)` |
| 5 | 2～6 | `OA(25,6,5,2)` |
| 5 | 7～8 | `OA(50,11,5,2)` |

规则：

1. 参考表因子数大于输入因子数时，取前 N 列；
2. 将矩阵中的 `0...s-1` 映射为用户输入的水平名称；
3. 每份内置表必须通过严格正交性验证；
4. 找不到参考表时，`auto` 降级到两两覆盖；`orthogonal` 明确报错。

参考来源：

- Sloane Orthogonal Array Library：<https://neilsloane.com/oadir/>
- NIST Covering Array Tables：<https://math.nist.gov/coveringarrays/>

### 8.2 V0.2 下载与本地保存要求

V0.1 不下载任何参考数据。进入 V0.2 时，实现一个手动执行的下载脚本：

```text
scripts/fetch_orthogonal_references.py
```

该脚本下载以下首批严格 OA 原始表，并原样保存到：

```text
reference_data/orthogonal_arrays/
```

| 本地文件 | Sloane 原始文件 URL |
| --- | --- |
| `oa.4.3.2.2.txt` | `https://neilsloane.com/oadir/oa.4.3.2.2.txt` |
| `oa.8.7.2.2.txt` | `https://neilsloane.com/oadir/oa.8.7.2.2.txt` |
| `oa.12.11.2.2.txt` | `https://neilsloane.com/oadir/oa.12.11.2.2.txt` |
| `oa.9.4.3.2.txt` | `https://neilsloane.com/oadir/oa.9.4.3.2.txt` |
| `oa.18.7.3.2.txt` | `https://neilsloane.com/oadir/oa.18.7.3.2.txt` |
| `oa.27.13.3.2.txt` | `https://neilsloane.com/oadir/oa.27.13.3.2.txt` |
| `oa.16.5.4.2.txt` | `https://neilsloane.com/oadir/oa.16.5.4.2.txt` |
| `oa.32.9.4.2.txt` | `https://neilsloane.com/oadir/oa.32.9.4.2.txt` |
| `oa.25.6.5.2.txt` | `https://neilsloane.com/oadir/oa.25.6.5.2.txt` |
| `oa.50.11.5.2.txt` | `https://neilsloane.com/oadir/oa.50.11.5.2.txt` |

脚本还必须生成：

```text
reference_data/orthogonal_arrays/catalog.json
```

其中记录每份文件的：

- `array_id`；
- 原始 URL；
- 下载日期；
- SHA-256；
- 行数、列数、水平数、强度；
- 本地正交性验证结果。

下载或验证失败的表不得写入可用目录；`orthogonal` 模式只能选择验证通过的表。

## 9. 两两覆盖生成要求

两两覆盖生成器必须：

1. 支持每个因子拥有不同数量的水平；
2. 保证任意两个因子的每一对水平至少覆盖一次；
3. 输出覆盖率和未覆盖组合；
4. 相同输入得到相同输出；
5. 不承诺行数绝对最少；
6. 生成完成后自行验证覆盖率必须为 `1.0`。

## 10. 命令行使用方式

```powershell
python -m combinatorial_test_table_generator `
  --input factors.json `
  --format markdown `
  --output case_table.md
```

强制只使用严格正交表：

```powershell
python -m combinatorial_test_table_generator `
  --input factors.json `
  --mode orthogonal `
  --format csv `
  --output case_table.csv
```

## 11. 推荐项目结构

```text
combinatorial-test-table-generator/
├── combinatorial_test_table_generator/
│   ├── command_line_interface.py
│   ├── input_schema.py
│   ├── pairwise_covering_generator.py
│   ├── orthogonal_array_selector.py
│   ├── orthogonal_array_validator.py
│   ├── output_formatters.py
│   └── models.py
├── reference_data/orthogonal_arrays/
├── unit_tests/
├── examples/
├── README.md
└── pyproject.toml
```

## 12. 版本计划

### V0.1：可用核心

- JSON 输入；
- `pairwise` 模式；
- 两两覆盖率验证；
- JSON、Markdown、CSV 输出；
- 单元测试；
- 不支持业务约束与严格 OA。

验收：任意 2～8 个、每因子 2～10 个水平的普通输入均可生成两两覆盖表。

### V0.2：严格正交表优先

- 内置常见等水平 OA 参考数据；
- `orthogonal` 与 `auto` 模式；
- OA 参数匹配、列投影、严格均衡验证；
- 输出 OA 来源与降级原因。

验收：常见 2～8 因子、2～5 等水平输入可优先返回对应严格 OA。

### V0.3：业务约束

- 支持 `forbidden_combinations`；
- 生成满足约束的两两覆盖表；
- 报告不合法与无法覆盖的组合。

### V0.4：Skill 包装

- 创建 Codex Skill；
- Skill 将自然语言因子整理为 JSON 并调用核心程序；
- 核心程序仍可脱离 Skill 独立运行。

## 13. 验收标准

- 两两模式下，覆盖率必须为 100%；
- 相同输入必须生成相同结果；
- 严格 OA 模式下，任意两列的每种水平组合出现次数必须相同；
- `auto` 模式不得把普通两两覆盖表标为严格 OA；
- 找不到 OA 时必须说明原因；
- 输入不合法时必须输出可理解的中文错误信息；
- 内置 OA 参考数据必须有来源链接和自动校验测试。
